{
   "name": "accessly",
   "version": "1.0.0",
   "scripts": {
      "lint": "cross-env vendor/bin/phpcs --standard=phpcs.xml",
      "format": "cross-env vendor/bin/php-cs-fixer fix"
   },
   "devDependencies": {
      "cross-env": "^7.0.3",
      "prettier": "^2.7.1"
   }
}
